export type Testimonial = {
    id: number;
    text: string;
    name: string;
    createdAt: string;
};

export type Product = {
    id: number;
    img: string;
    title: string;
    description: string;
    price: string;
};

export type Slide = {
    id: number;
    img: string;
}